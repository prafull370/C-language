#include<stdio.h>
#include<stdlib.h>
 int stack[100],choice=0,front=-1,rear=-1,s,v;
 insert(){
    printf("Enter value:");
    scanf("%d",&val);
    front=
 }
 int main(){
    printf("Enter the size of array:");
    scanf("%d",&s);
    while(1){
        printf("\n1. Insertion in Queue:");
        printf("\n2. Deletion in Queue:");
        printf("\n1. Show in Queue:");
        printf("\n1. Exit:");
        printf("Enter your choice:");
        scanf("%d",&choice);
        switch(choice){
            case 1: insert();
            break;
            case 2: delete();
            break;
            case 3: showQueue();
            break;
            case 4: exit(1);
            break;
            default: printf("Invalid choice.");
        }
    }
 }


 //TOh
 void(int a,int b){
    if(n==1){
        printf("move fronm %c to%c",a,b);

    }
    else{
        TOH(n-1,a,c,b){
            printf("move to%c to %c",a,b);
            TOH(n-1,c,b,a);
        }
    int main(){
        int n;
    }
 }
 }